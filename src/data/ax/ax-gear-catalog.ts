import type { Product } from "@/data/products";
import data from "./ax-gear-catalog.json";

/** Auto-generated — thin wrapper over JSON catalogue. */
export const axGearCatalogProducts = data as unknown as Product[];
