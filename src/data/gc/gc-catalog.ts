import type { Product } from "@/data/products";
import data from "./gc-catalog.json";

/** Auto-generated — Gucci handbags + men's bags + women's/men's RTW + women's/men's shoes + wallets + men's wallets + fashion accessories + women's/men's travel + jewellery + gifts. */
export const gcCatalogProducts = data as unknown as Product[];
